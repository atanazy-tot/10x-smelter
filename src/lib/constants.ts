/**
 * Shared constants for the Smelter application.
 */

/** Daily smelt limit for anonymous (non-authenticated) users */
export const ANONYMOUS_DAILY_LIMIT = 1;

/** Default weekly credits for authenticated users without API key */
export const WEEKLY_CREDITS_DEFAULT = 5;
